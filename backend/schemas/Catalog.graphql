# @format

# // Types

type Catalogs {
	_id: String
	name: String
	active: Boolean
}

type Catalog {
	_id: String
	name: String
	active: Boolean
	products: [Product]
}

# // INPUTS

input CatalogInput {
	name: String!
}

input CatalogInputUpdate {
	name: String
}

input CatalogFilter {
	_id: String
	name: String
}
